package com.example.agricitytest2

import androidx.appcompat.app.AppCompatActivity

private const val TAG = "GraphTypeClass"

enum class GraphType{
    LINE, PIE, BAR
}
class Graph(val domain: String) : AppCompatActivity() {



}